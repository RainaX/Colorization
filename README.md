# Colorization

Directory:
          train_small
          train
          val
          test



pip3 install -r requirements.txt

rm -r checkpoints

./scripts/train_siggraph.sh

Modify: root, phase.
